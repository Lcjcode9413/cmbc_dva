import React,{useState,useEffect,useContext} from 'react';


const One = () => {
  console.log('one')
  // const { name } = useContext(TestContext)
  return (
    <div>
      this is one
    </div>
  );
};

One.propTypes = {
};

export default One;
